extends base

block content
    h2 Log in

    form.form(action="" method="POST")
        input(type="text" name="email" placeholder="email")
        input(type="password", name="password" placeholder="password")
        if error
            p.error= error
        button
            a.button.disabled Log in
